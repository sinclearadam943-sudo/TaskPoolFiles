# 微服务编排系统设计

## 整体架构

微服务编排系统管理多个微服务之间的调用流程，处理服务发现、负载均衡、容错、分布式事务。

```
┌─────────────────────────────────────────────────────────────┐
│         编排引擎 Orchestration Engine                         │
│  根据工作流定义，按顺序调用各个微服务                             │
├─────────────────────────────────────────────────────────────┤
│              服务注册发现                                       │
│  - 服务启动注册                                               │
│  - 心跳检测                                                   │
│  - 服务下线自动移除                                           │
├─────────────────────────────────────────────────────────────┤
│              负载均衡                                         │
│  轮询 / 加权轮询 / 最少连接                                    │
├─────────────────────────────────────────────────────────────┤
│              容错机制                                         │
│  - 熔断器 (断路 / 半开 / 关闭)                                  │
│  - 降级（返回默认值）                                         │
│  - 超时重试                                                   │
├─────────────────────────────────────────────────────────────┤
│              分布式事务                                       │
│  SAGA模式 / 两阶段提交                                        │
└─────────────────────────────────────────────────────────────┘
```

## 服务注册与发现

### 核心数据

每个服务实例存储：

```json
{
  "service_name": "payment-service",
  "instances": [
    {
      "instance_id": "payment-001",
      "host": "192.168.1.10",
      "port": 8080,
      "status": "healthy",
      "last_heartbeat": 1678901234,
      "metadata": {
        "version": "v1.0.0",
        "zone": "dc1"
      }
    }
  ]
}
```

### 心跳机制

- 每个实例每隔 N 秒发送心跳
- 如果 M 秒没收到心跳，标记为不健康，从列表移除

## 负载均衡策略

| 策略 | 说明 |
|------|------|
| 轮询 | 依次选择，简单公平 |
| 加权轮询 | 根据实例权重分配，高配更多 |
| 最少连接 | 选当前连接数最少的 |
| 哈希 | 根据请求IP哈希，同一个IP总落到同一实例 |

## 服务熔断和降级

### 熔断器状态机

```
          ┌─────────────┐
          │   关闭      │  正常服务
          └──────┬──────┘
                 │ 失败率超阈值
                 ▼
          ┌─────────────┐
          │   打开      │  直接失败，不走网络
          └──────┬──────┘
                 │ 等待冷却时间
                 ▼
          ┌─────────────┐
          │   半开      │  允许少量请求尝试
          └──────┬──────┘
                 │
       ┌─────────┴─────────┐
       │                  │
    成功                失败
       │                  │
       ▼                  ▼
┌─────────────┐    ┌─────────────┐
│   关闭      │    │   打开      │
└─────────────┘    └─────────────┘
```

### 降级策略

- 服务超时/熔断后，返回预定义的默认值
- 比如：推荐商品服务挂了，返回热门商品列表

## 分布式事务 - SAGA 模式

SAGA 适合长事务，把大事务分成多个本地事务：

```
order-service 创建订单 → (成功) → payment-service 扣钱 → (成功) → stock-service 减库存
                          ↓ (失败)                       ↓ (失败)
                补偿: order-service 取消订单       补偿: payment-service 退款
```

每个步骤有对应的补偿操作，任何一步失败，按逆序调用补偿操作回滚。

优点：
- 允许各个微服务独立开发
- 适合长事务
- 可用性好，不需要锁

缺点：
- 不保证强一致性，最终一致
- 需要业务实现补偿接口

## 分布式事务 - 两阶段提交 (2PC)

阶段一：协调者向所有参与者问能不能提交
阶段二：如果都可以，就提交；否则都回滚

优点：强一致性
缺点：
- 协调者单点问题
- 阻塞协议，性能不好
- 出现网络分区会导致数据不一致

生产环境微服务推荐 SAGA，互联网不追求强一致，追求高可用。

## 服务间通信

支持两种通信模式：

1. **同步调用**：HTTP/gRPC，等待返回
2. **异步消息**：消息队列，发完就走，后续回调

编排适合同步流程，一个个按顺序来；事件驱动适合异步解耦。

## 最佳实践

1. **尽量小粒度**：一个服务一个职责，不要太大
2. **独立部署**：每个服务可以独立发布不影响别人
3. **容错设计**：一个服务挂了不影响整个系统雪崩
4. **可观测**：每个服务调用都有链路追踪，出问题能找到
5. **最终一致**：分布式不要追求强一致，很难做，可用性更重要

---
设计由 oc-coze 于 2026-03-16 生成
